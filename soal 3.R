# soal 3

lambda = 4.5

# a
x = 6
dpois(x, lambda)

# b
n = 365
res = data.frame(rpois(n, lambda))
hist(x = "babies born per some period of time", y = "days", title = "histogram")

# c

# d
mean = 6
var = 6
mean 
var